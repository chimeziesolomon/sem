import React from 'react';
import './default.scss';

const Footer = (props) => {
	return (
		<footer className="footer">
			<div className="wrap">@ Sem 2021</div>
		</footer>
	);
};

export default Footer;
